#ifndef LX_LANOX2D_CONFIG_H
#define LX_LANOX2D_CONFIG_H

// version
#define LX_CONFIG_VERSION "${VERSION}-${GIT_COMMIT}"
#define LX_CONFIG_VERSION_MAJOR ${VERSION_MAJOR}
#define LX_CONFIG_VERSION_MINOR ${VERSION_MINOR}
#define LX_CONFIG_VERSION_ALTER ${VERSION_ALTER}
#define LX_CONFIG_VERSION_BUILD ${VERSION_BUILD}

// defines
#define LX_CONFIG_OS_${OS} 1
${define LX_CONFIG_SMALL}
${define LX_CONFIG_TYPE_HAVE_WCHAR}

// windows
${define LX_CONFIG_WINDOW_HAVE_SDL}

// devices
${define LX_CONFIG_DEVICE_HAVE_BITMAP}

#endif
